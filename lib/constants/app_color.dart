
import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  AppColors._();

  // --- Backgrounds ---
  static const Color backgroundLight = Color(0xFFF0EEE9);

  static const Color buttonColor = Color(0xFF38B6FF);
  static const Color captureButtonColor = Color(0xFFF19243);
}